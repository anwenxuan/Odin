id: repo_map
version: 1.0.0
name: Repo Map
description: Build a module map and language distribution for the target repository.
owner: core-team
tags:
  - discovery
  - modules
runtime:
  model_profile: default
  timeout_sec: 60
contracts:
  prompt: prompt.md
  input_schema: input_schema.json
  output_schema: output_schema.json
requirements:
  evidence_required: true
