id: sink_detection
version: 1.0.0
name: Sink Detection
description: Identify dangerous sinks such as SQL execution, command execution, and deserialization.
owner: core-team
tags: [security, vulnerability, sinks]
runtime:
  model_profile: default
  timeout_sec: 60
contracts:
  prompt: prompt.md
  input_schema: input_schema.json
  output_schema: output_schema.json
requirements:
  evidence_required: true
